# NumberGuessingGame
Random number generator guessing game made in Scheme. Needs Guile compiler to run. This simple project uses gettimeofday as a seeding method for my-random number generator. User is then instructed to guess numbers and is given hints until the correct number is chosen.
